<Window x:Class="Hotel.View.Windows.AdministratorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hotel.View.Windows"
        mc:Ignorable="d"
        Title="Окно администратора" MinHeight="720" MinWidth="1280" 
        WindowStartupLocation="CenterScreen">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Name="UserBtn" Content="Пользователи"
                    Click="UserBtn_Click"/>
            <Separator Width="20" Height="0"/>
            <Button Name="RoomsBtn" Content="Комнаты"
                    Click="RoomsBtn_Click"/>
        </StackPanel>

        <Frame Name="MainFrame" Grid.Row="2"
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
